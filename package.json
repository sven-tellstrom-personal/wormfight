{
  "name": "wormfight",
  "version": "2.0.0",
  "description": "Online multiplayer worm fight — up to 20 players",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "deploy": "gcloud run deploy wormfight --source . --region europe-west1 --allow-unauthenticated --port 8080 --project wormfight-2fac5"
  },
  "dependencies": {
    "express": "^4.21.2",
    "socket.io": "^4.8.1"
  }
}
